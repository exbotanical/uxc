union Result = Message | PrivateThread

type Query {
	search(query: String): [Result]
}

type SeedReturnValue {
	user: User
	threadIds: [ID]
	userIds: [ID]
}

type Mutation {
	seed: SeedReturnValue
	purge: Boolean
}
